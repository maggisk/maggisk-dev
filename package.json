{
  "name": "maggisk",
  "version": "1.0.0",
  "scripts": {
    "start": "elm-live src/Main.elm --pushstate --port 1234 --dir=static -- --debug --output=static/app.js",
    "preview": "elm-live src/Main.elm --pushstate --dir=static --open -- --output=static/app.js",
    "format": "elm-format src/ --yes"
  },
  "license": "ISC",
  "dependencies": {
    "elm": "^0.19.1-3",
    "uglify-js": "^3.7.7"
  },
  "devDependencies": {
    "elm-format": "^0.8.2",
    "elm-live": "^4.0.1"
  }
}
